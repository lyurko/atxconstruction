{
  "name": "atx-construction",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://lyurko.github.io/atxconstruction",
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -b master -d build"
  },
  "devDependencies": {
    "flow-bin": "^0.76.0",
    "gh-pages": "^1.2.0"
  }
}
